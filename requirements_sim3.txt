# tanGo sim3 — dependencias de RL
# Instalar con: pip install -r tests/sim3/requirements_sim3.txt

# Core RL
ray[rllib]>=2.9.0
torch>=2.1.0
gymnasium>=0.29.0

# Ya deberían estar instaladas del proyecto base
# pero se listan para completitud:
networkx>=3.0
numpy>=1.24.0
folium>=0.14.0
plotly>=5.0.0
requests>=2.28.0